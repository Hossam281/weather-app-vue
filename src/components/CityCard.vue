<template>
  <div class="flex py-6 px-3 bg-[#222831] hover:bg-[#eeeeee] hover:text-[#393E46] rounded-lg shadow-xl cursor-pointer hover:scale-105 transition-all duration-300">
    <div class="flex flex-col flex-1">
      <h2 class="font-bold text-3xl">{{ city.state }}</h2>
    </div>
    <div class="flex flex-col gap-3">
      <p class="text-3xl self-end">
        {{ Math.round(city.weather.main.temp ) }}&deg;
      </p>

      <div class="flex gap-2">
        <span class="text-xs">
          H:
          {{ Math.round(city.weather.main.temp_max) }}&deg;
        </span>
        <span class="text-xs">
          L:
          {{ Math.round(city.weather.main.temp_min) }}&deg;
        </span>
      </div>
    </div>
  </div>
</template>

<script setup>
defineProps({
  city: {
    type: Object,
    default: () => {},
  },
});
</script>
